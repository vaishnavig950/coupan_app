const add = require('./addCoupan');
const apply = require('./applyCoupan');
const verify = require('./verifyCoupan');
const status = require('./coupanStatus');

module.exports = {
    add,
    apply,
    verify,
    status
}